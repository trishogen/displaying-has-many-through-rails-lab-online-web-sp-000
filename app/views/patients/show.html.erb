<h1> Appointments: </h1>
<% @patient.appointments.each do |a| %>
  <p>
  Day and time: <%= a.appointment_datetime.strftime("%B %e, %Y at %H:%M") %> </br>
  Doctor:  <%= link_to a.doctor.name, doctor_path(a.doctor) %> </br>
  </p>
<% end %>
